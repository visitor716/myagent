interface:
  display_name: "Prompt Optimizer"
  short_description: "Rewrite prompts for clearer agent execution"
  default_prompt: "Use $prompt-optimizer to improve this prompt without executing it."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "WSL Windows Chrome"
  short_description: "Attach from WSL to a dedicated Windows automation browser."
  default_prompt: "Use $wsl-windows-chrome to attach from WSL to the dedicated Windows Chrome or Edge automation browser before opening a fresh browser."

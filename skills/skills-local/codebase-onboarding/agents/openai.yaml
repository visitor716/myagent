interface:
  display_name: "Codebase Onboarding"
  short_description: "Map an unfamiliar repo for maintainers"
  default_prompt: "Use $codebase-onboarding to generate an architecture and workflow map for this repo."
policy:
  allow_implicit_invocation: true

openai:
  display_name: "my-TG App Release Command"
  short_description: "Make /app send latest WebApp version, branch, deploy time, and notes."
  default_prompt: "Use $my-tg-app-release-command to implement or verify tg-agent-gateway /app, /webapp, and /latest_app release messages with branch-specific version fingerprints, deployment time, latest update notes, and fresh WebApp buttons."

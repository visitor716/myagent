interface:
  display_name: "TDD Workflow"
  short_description: "Run test-first feature and bug-fix loops"
  default_prompt: "Use $tdd-workflow to implement this change with test-first verification."
policy:
  allow_implicit_invocation: true

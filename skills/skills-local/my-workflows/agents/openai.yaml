interface:
  display_name: "my-workflows"
  short_description: "Coordinate tg-agent-gateway cx1 planning, cx2 orchestration/review, worker execution, and master handoff."
  default_prompt: "Use $my-workflows for tg-agent-gateway cx1->cx2->worker->observer->cx2 review->master integration workflows. Load the referenced worker-dispatch, review-integration, or handoff template files only when that lane is active."
policy:
  allow_implicit_invocation: true

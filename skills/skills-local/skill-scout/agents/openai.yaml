interface:
  display_name: "Skill Scout"
  short_description: "Find existing skills before creating new ones"
  default_prompt: "Use $skill-scout to check whether a skill already exists for this workflow."
policy:
  allow_implicit_invocation: true

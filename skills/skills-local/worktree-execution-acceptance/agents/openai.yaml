interface:
  display_name: "my-Worktree Execution Acceptance"
  short_description: "Validate real worktree execution cwd and tg-agent-gateway worker isolation"
  default_prompt: "Use $my-worktree-execution-acceptance to verify whether this repository's agent runner actually executes tasks inside Git worktrees, including tg-agent-gateway effectiveWorkspace and worktreePath evidence."

policy:
  allow_implicit_invocation: true

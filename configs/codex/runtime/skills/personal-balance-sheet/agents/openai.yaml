interface:
  display_name: "Personal Balance Sheet"
  short_description: "Update, merge, and organize balance workbooks"
  default_prompt: "Use $personal-balance-sheet to update, merge, split, archive, convert, or organize BalanceSheet workbooks and screenshots."
policy:
  allow_implicit_invocation: true
